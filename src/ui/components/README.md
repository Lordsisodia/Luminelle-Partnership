# src/ui/components

Shared, domain-agnostic UI used throughout the app (global header/footer, SEO helpers, section headings, etc).
Always import from the `@ui/components/*` alias to keep domain code free from deep relative paths.
