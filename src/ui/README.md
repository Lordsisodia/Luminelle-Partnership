# src/ui

Shared UI surface for cross-domain primitives. Anything needed by multiple domains (layouts, providers, global nav/footer, shared components) should live here and be imported via the `@ui/*` alias.
