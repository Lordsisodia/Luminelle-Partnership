# Admin Settings
Purpose: Admin settings surfaces (feature flags, integrations, org config).

Route:
- /admin/settings (tbd)

Data sources:
- Feature flags, environment configs, Supabase tables.

Key modules:
- ui/ exists; add data/logic/hooks as settings are built.

Roadmap:
- Define settings schema, permissions, audit trail.
