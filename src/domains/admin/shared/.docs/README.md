# Admin Shared
Purpose: Cross-admin utilities and presentation shared by all admin subdomains.

Includes:
- ui/ (layouts, components, sections, pages entrypoints)
- data/, hooks/, logic/ (reusable helpers)

Dependencies:
- Clerk auth, Supabase, design tokens.

Roadmap:
- Extract more shared view-model hooks; document import conventions.
