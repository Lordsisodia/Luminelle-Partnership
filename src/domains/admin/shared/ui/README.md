# Admin UI (shared)

Shared presentation layer for admin:
- layouts/ (shell, page layout)
- components/ (guards, nav, shared widgets)
- preview/ (device frames, iPhone mockup, preview cards)
- pages/ (route entry points; per-subdomain data/hooks live alongside each subdomain)
- sections/ (shared sections)
