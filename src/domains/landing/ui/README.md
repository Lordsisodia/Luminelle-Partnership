# landing/ui
View layer for the landing domain: pages, sections, components, layouts. If it renders HTML, it lives here; behavior/data lives in logic/data. Import via `@landing/ui/*`.

Archived/parked pieces now live in `src/archive/landing/ui` (legacy sections/components kept for reference).
