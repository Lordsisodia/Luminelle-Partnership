# landing/data
Content and data access for landing (static content, queries, fetchers). Keep UI-free.

Note: authoritative marketing content now lives in `src/content/*`. Older/duplicate configs are kept in `src/archive/landing/data/` to prevent drift while retaining history.
