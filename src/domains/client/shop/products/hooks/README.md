# Products hooks

Domain-specific hooks (data fetching/merging, UI glue) that are reusable across product pages/sections.

Examples:
- useProductContent (merge config + loaders, guard stubs)
- useGallery (image/video selection, encoding)
