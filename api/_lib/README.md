# server/
Express webhook/dev server for local integration tests (Shopify/Clerk/Supabase). Not shipped to the client bundle. Keep API logic domain-agnostic; per-domain handlers should live with the domain and be imported here.
