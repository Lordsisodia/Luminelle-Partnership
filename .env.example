# Copy this file to `.env` for local dev.
# This file contains placeholders only — never commit real secrets.
#
# NOTE: Vite only exposes variables prefixed with `VITE_` to the browser.
# Everything else is server-only and should be set as platform secrets (Vercel env, Cloudflare Worker secrets, etc.).

############################################################
# Frontend (Vite / Cloudflare Pages)
############################################################

# Shopify Storefront (browser)
VITE_SHOPIFY_STORE_DOMAIN=
VITE_SHOPIFY_STOREFRONT_PUBLIC_TOKEN=

# Volume discount codes (browser)
# Used to auto-apply tiered quantity discounts via `cartDiscountCodesUpdate`.
# If unset, the app defaults to CAP2SAVE5 (2+), CAP3SAVE10 (3+), and CAP4SAVE15 (4+) for the shower cap.
VITE_SHOWER_CAP_DISCOUNT_CODE_QTY2=
VITE_SHOWER_CAP_DISCOUNT_CODE_QTY3=
VITE_SHOWER_CAP_DISCOUNT_CODE_QTY4=

# Cart mode (recommended for Cloudflare Pages: keep normal shoppers off your API)
VITE_USE_SERVER_CART=0

# Supabase (browser, safe-to-expose anon key)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Clerk (browser)
VITE_CLERK_PUBLISHABLE_KEY=
# Optional: Clerk JWT template name used for Supabase auth (defaults to `supabase`)
VITE_CLERK_SUPABASE_JWT_TEMPLATE=

# Optional: admin access bootstrap (comma-separated emails)
VITE_ADMIN_EMAILS=

# Optional: marketing links
VITE_WHATSAPP_INVITE_URL=
VITE_WHATSAPP_SUPPORT_PHONE=   # e.g. 447700900000 (E.164 digits, no +)
VITE_WHATSAPP_SUPPORT_TEXT=    # optional prefilled message
# Legacy/alternate format (if you already have a full WhatsApp URL):
VITE_WHATSAPP_SUPPORT_URL=
VITE_CONTENT_BRIEF_URL=

# Optional: asset CDN toggle
VITE_USE_ASSET_CDN=0
VITE_ASSET_BASE_URL=

# Optional: cart recovery UI toggles (feature-gated)
VITE_CART_RECOVERY_ENABLED=
VITE_CART_SHARE_ENABLED=

############################################################
# Analytics / Experiments (browser)
############################################################

# Kill switches (default off)
VITE_EXPERIMENTS_ENABLED=false
VITE_ANALYTICS_ENABLED=false
VITE_HEATMAP_ENABLED=false

# PostHog (browser SDK)
VITE_POSTHOG_KEY=
VITE_POSTHOG_HOST=   # e.g. https://us.i.posthog.com or https://eu.i.posthog.com (PostHog ingestion host)

# Microsoft Clarity (heatmaps/session replay)
VITE_CLARITY_PROJECT_ID=

# Meta (Facebook) Pixel - for ad attribution and retargeting
# Optional: Only needed if running Facebook/Instagram ads
# Get your pixel ID from: https://business.facebook.com/data_sources/pixels
VITE_META_PIXEL_ID=1048422446793291

# Meta Conversions API (CAPI) - server-side event tracking
# Required for sending Purchase events with correct Content ID format
# Get your access token from: https://business.facebook.com/events_manager/
# Click your pixel → Settings → Conversions API → Generate access token
VITE_META_CAPI_TOKEN=
META_CAPI_ACCESS_TOKEN=

############################################################
# Server/API (Vercel or Workers) — secrets
############################################################

# If you deploy the Node/Vercel-style API (`api/**`)
DATABASE_URL=

# Shopify App + Webhooks
SHOPIFY_STORE_DOMAIN=
SHOPIFY_API_KEY=
SHOPIFY_API_SECRET=
SHOPIFY_API_VERSION=
SHOPIFY_SCOPES=

# Public app URL used for OAuth redirects.
# Some older docs/scripts may refer to APP_URL; keep both if needed.
SHOPIFY_APP_URL=
APP_URL=

# Shopify webhook verification secret.
#
# IMPORTANT: Shopify signs Admin API webhooks using your app's **API secret / client secret**.
# There is no separate “webhook secret” you generate in the Shopify UI.
#
# This project historically used `SHOPIFY_WEBHOOK_SECRET` in some deployments; keep it set
# to the SAME value as `SHOPIFY_API_SECRET` (or leave it empty if your deployment uses
# `SHOPIFY_API_SECRET` directly).
SHOPIFY_WEBHOOK_SECRET=

# PostHog server-side capture (Shopify webhooks → purchase event)
# This can be the same project key as `VITE_POSTHOG_KEY`, but keep it server-side.
POSTHOG_API_KEY=
POSTHOG_HOST=        # e.g. https://us.i.posthog.com or https://eu.i.posthog.com (PostHog ingestion host)

# Supabase (server-side)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Cloudinary signing auth (optional but recommended)
ADMIN_SHARED_SECRET=

# Optional: internal auth / email
INTERNAL_SHARED_SECRET=
EMAIL_SEND=

# Shopify Customer Accounts (server-side)
CUSTOMER_CLIENT_ID=
CUSTOMER_CLIENT_SECRET=

# Clerk webhooks (server-side)
CLERK_WEBHOOK_SECRET=

# Cloudinary (server-side; do not expose API_SECRET to browser)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_URL=
# Optional public values (browser): use a preset that allows signed uploads.
VITE_CLOUDINARY_CLOUD_NAME=
VITE_CLOUDINARY_UPLOAD_PRESET=
VITE_CLOUDINARY_API_KEY=
