# Platform feedback index

Living feedback docs for platform-level user flows (auth, callbacks, shared shells).

Source of truth for current routes: `src/App.tsx`.

## Auth
- Sign in (`/sign-in`) → [sign-in.md](./sign-in.md)
- Sign up (`/sign-up`) → [sign-up.md](./sign-up.md)
- SSO callback (`/sso-callback`) → [sso-callback.md](./sso-callback.md)

