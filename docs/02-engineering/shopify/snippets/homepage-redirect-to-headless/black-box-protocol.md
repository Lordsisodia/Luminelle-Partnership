# Black Box Protocol (Docs)

Canonical protocol doc lives at:

- `docs/.blackbox/protocol.md`

Legacy version (kept for historical context):

- `docs/.blackbox/experiments/legacy/protocol-legacy.md`

## 📌 Docs structure rule

- ✅ `.blackbox/` should only exist at `docs/.blackbox/` (hidden at the docs root).
- ❌ Do not create nested `.blackbox/` folders inside `docs/06-quality/` or other visible docs areas.
