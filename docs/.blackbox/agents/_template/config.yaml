id: "<agent-id>"
name: "<agent-name>"
version: 1
owner: "team"

io:
  inputs:
    - name: "goal"
      required: true
  outputs:
    - name: "plan"
      path: ".plans/<timestamp>_<slug>/"

behavior:
  prefers_plan: true
  writes_journal: true

