# Core Agent Prompt

This folder contains the **shared operating prompt** that all agents should follow when running inside a `.blackbox/`.

Use this as the “base prompt” and then layer agent-specific instructions on top.

