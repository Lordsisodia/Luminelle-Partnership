# Work Queue

## Next actions (keep 5–10)
- [ ] Write the 1-day mini‑POC plan (blog page kit + marketing sections + storefront slices)
- [ ] Fill `artifacts/run-meta.yaml` with inputs + outputs and current model
- [ ] Populate `artifacts/sources.md` with canonical doc paths + key repo URLs
- [ ] Update `artifacts/summary.md` with “build next” recommendations + stop rules
- [ ] Add a plan checkpoint step file under `context/steps/` capturing what changed
