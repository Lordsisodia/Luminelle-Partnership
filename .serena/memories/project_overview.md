## Lumelle affiliate landing
- Marketing site for the Lumelle creator/affiliate program built with React.
- Frontend served via Vite workspace (`app`) and deployable to Vercel (see `vercel.json`).
- Local `components-library` workspace mirrors `@siso/ui` for sharing design system components.
- Documentation for client requirements lives in `docs/` and `feedback/` holds qualitative notes.