## Task wrap-up checklist
- Run `npm run lint` if you touched TypeScript/React code.
- Run `npm run build` before deploy-facing changes to confirm Vite/TypeScript succeed.
- Update `docs/` with client feedback or decisions when relevant.
- Surface new assets or environment variables in `.env.example` if they change.